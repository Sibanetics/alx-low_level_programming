#include <stdio.h>
/**
 * main - Input and Outputing data
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[100] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
