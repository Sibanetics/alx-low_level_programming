#include <stdio.h>
#include <stdbool.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	bool bokoIsFat = true;

	printf("Is Boko Fat (1 is yes, 0 is no)? %i\n", bokoIsFat);
	
	printf("%i\n", bokoIsFat + 10);

	return (0);
}
